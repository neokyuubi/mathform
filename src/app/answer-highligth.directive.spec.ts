import { AnswerHighligthDirective } from './answer-highligth.directive';

describe('AnswerHighligthDirective', () => {
  it('should create an instance', () => {
    const directive = new AnswerHighligthDirective();
    expect(directive).toBeTruthy();
  });
});
